pre-commit:
  commands:
    lint-staged:
      run: pnpm lint-staged

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}

pre-push:
  commands:
    lint:
      run: pnpm lint
    format-check:
      run: pnpm format:check
    typecheck:
      run: pnpm typecheck
