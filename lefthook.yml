pre-commit:
  commands:
    lint-staged:
      run: pnpm lint-staged
    lint:
      run: pnpm lint
    format:
      run: pnpm format
    typecheck:
      run: pnpm typecheck

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}
